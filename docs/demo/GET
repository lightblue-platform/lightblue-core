#!/bin/bash

if [ "$#" == 2 ]; then
  upwd=$2
else
  upwd=${unamepwd}
fi

echo curl -H  Content-Type:application/json  -u ${upwd} -X GET $1
curl -H  Content-Type:application/json  -u ${upwd} -X GET $1> /tmp/out


mv /tmp/out /tmp/out.json


